name: Print Name from Environment Variable (dev)

on:
  push:
    branches: 
      - "**"
jobs:
  hello:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Print
        run: echo "My name is: ${{ vars.MY_NAME }}"